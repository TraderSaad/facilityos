# Architecture Overview

This monorepo contains frontend applications, backend services, shared packages, and infrastructure helpers for FacilityOS.

- `apps/*` — Next.js applications
- `services/*` — NestJS microservices
- `packages/*` — Shared code used across apps and services
- `prisma/` — Database schema and seed scripts
- `docker/` — Container startup and service orchestration
